import React from 'react';
import {LoaderSpan, LoaderWrapper} from "./Loader.styled";


export const Loader = () => {
    return (
        <LoaderWrapper>
            <LoaderSpan></LoaderSpan>
        </LoaderWrapper>
    );
};
