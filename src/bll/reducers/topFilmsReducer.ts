import {createSlice} from "@reduxjs/toolkit";


const initialState = {}

const slice = createSlice({
    name: 'topFilms',
    initialState: initialState,
    reducers: {
        setTopFilms(state, action) {
        }
    }
})